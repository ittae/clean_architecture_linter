name: clean_architecture_linter_example
description: Example project for testing Clean Architecture linter rules
version: 1.0.0

environment:
  sdk: ^3.0.0

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  custom_lint: ^0.7.6
  clean_architecture_linter:
    path: ../
  test: ^1.26.2

flutter:
  uses-material-design: true